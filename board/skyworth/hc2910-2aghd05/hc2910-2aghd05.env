// SPDX-License-Identifier: GPL-2.0-or-later
/*
 * Board env file for HC2910
 */

/* Store sciprts/pxe file/fdt etc.. to low address (below 0xC00000) */
/* Note 0xC00000 (12MiB) - 0xC40000 (12.25MiB) is occupied by Secure Monitor */
fdt_addr_r=0x0
fdtoverlay_addr_r=0x100000
fdtfile=hisilicon/hi3798mv200-hc2910-2aghd05.dtb
ipaddr=192.168.100.1
scriptaddr=0x200000
pxefile_addr_r=0x300000
// vmlinuz is compressed about 2.3x smaller, so split this 112MB into 2 parts 1:2.3 approx.
// 32MB + 80MB
// Should be more than adequate
kernel_addr_r=0x1000000
kernel_comp_size=0x5000000
kernel_comp_addr_r=0x3000000
ramdisk_addr_r=0x8000000
loadaddr=0x1000000

// The on-board router routes vlan10 to LAN port and vlan15 to WAN port
// Change this value or remove it if your board has tweaks
vlan=10


// a default partition layout
// 2 partitions: boot(256MB) + rootfs(rest)
// the first default 1MiB gap is usually adequate to hold bootloaders
mbr_parts=name=boot,start=1M,size=256M,bootable,id=0x0e; \
	name=rootfs,size=-,id=0x83;
